# Coolify self-host stack for Autonoma (core / "preview" topology).
#
# Mirrors what Autonoma runs for its own preview envs (.preview.yaml): db +
# redis + temporal + api + ui, PREVIEWKIT_ENV=true for email/password login,
# prisma migrate on boot. Phase-2 test-execution workers are now included below
# (worker-general/web/diffs) — the API dispatches Temporal workflows, they consume.
#
# Coolify's docker-compose build pack does NOT bind-mount the repo, so the
# source is baked into an image (Dockerfile.coolify). api + ui each build it.
# Runs dev servers (tsx watch / vite), not the CI Dockerfiles (which expect a
# prebuilt dist/). Migrations run in the api command on boot.
#
# ui is exposed publicly via Coolify's SERVICE_FQDN_UI_3000 magic — the only
# mechanism that both wires the right-port traefik router AND joins the coolify
# proxy network. It proxies /v1 to the api container (same-origin, no CORS).

# ---------------------------------------------------------------------------
# Phase-2 test-execution workers (2026-07-21). The API dispatches Temporal
# workflows onto 5 task queues; these consumers run them. MVP set = general
# (hosts workflow code + generation) + web (browser step execution, Playwright
# image) + diffs (visual-diff refinement). investigation + mobile deferred.
#
# worker-web is a run-once "job pod": it exits after one activity (or 120s
# idle), so restart:always makes Coolify re-spawn it per job — the poor-man's
# K8s pod-per-test. general/diffs are long-lived workflow workers.
# ---------------------------------------------------------------------------
x-worker-env: &worker-env
  NODE_ENV: production
  DATABASE_URL: postgresql://postgres:postgres@postgres:5432/autonoma
  REDIS_URL: redis://redis:6379
  TEMPORAL_ADDRESS: temporal:7233
  TEMPORAL_NAMESPACE: default
  GEMINI_API_KEY: ${GEMINI_API_KEY:-placeholder}
  GROQ_KEY: ${GROQ_KEY:-placeholder}
  OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-placeholder}
  OPENROUTER_BASE_URL: ${OPENROUTER_BASE_URL:-}
  GEMINI_BASE_URL: ${GEMINI_BASE_URL:-}
  S3_BUCKET: ${S3_BUCKET:-autonoma}
  S3_REGION: ${S3_REGION:-us-east-1}
  S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:-unused}
  S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:-unused}
  S3_ENDPOINT: ${S3_ENDPOINT:-}

# general + diffs require valid GITHUB_APP_* at boot (no LOCAL_DEV bypass in the
# worker env schema). GitHub is only actually called for repo-backed scenarios;
# a throwaway base64 PEM satisfies boot validation for URL-based web tests.
x-github-env: &github-env
  GITHUB_APP_ID: ${GITHUB_APP_ID:-1}
  GITHUB_APP_PRIVATE_KEY: ${GITHUB_APP_PRIVATE_KEY:?set in Coolify}
  GITHUB_APP_SLUG: ${GITHUB_APP_SLUG:-autonoma}
  GITHUB_APP_WEBHOOK_SECRET: ${GITHUB_APP_WEBHOOK_SECRET:-ddash}

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: autonoma
    volumes:
      # Postgres 18+ rejects a mount at .../data ("unused mount/volume"); the
      # mount must be at /var/lib/postgresql (data lands in a version subdir).
      - pgdata18:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 30s

  redis:
    image: redis:alpine
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 15

  temporal:
    image: temporalio/auto-setup:1.25.2
    environment:
      DB: postgres12
      DB_PORT: "5432"
      POSTGRES_USER: postgres
      POSTGRES_PWD: postgres
      POSTGRES_SEEDS: postgres
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      # auto-setup binds the frontend to the container IP, not 127.0.0.1, so the
      # default (localhost) probe always refuses -> container never reports
      # healthy. Target the container's own IP.
      test: ["CMD-SHELL", "temporal operator cluster health --address $(hostname -i):7233"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 40s

  temporal-ui:
    image: temporalio/ui:2.34.0
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: ${APP_URL:-http://localhost:3000}
    depends_on:
      - temporal

  api:
    build:
      context: .
      dockerfile: Dockerfile.coolify
    working_dir: /app
    environment:
      # dev NODE_ENV keeps better-auth's crossSubDomainCookies OFF, so the
      # session cookie is host-only on the ui origin (same-origin via proxy).
      NODE_ENV: development
      API_PORT: "4000"
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/autonoma
      REDIS_URL: redis://redis:6379
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_NAMESPACE: default
      PREVIEWKIT_ENV: "true"       # enables email/password auth (no Google needed)
      LOCAL_DEV: "true"            # fake GitHub app so boot needs no GH creds
      STRIPE_ENABLED: "false"
      INTERNAL_DOMAIN: ${INTERNAL_DOMAIN:-autonoma.app}
      APP_URL: ${APP_URL:-http://localhost:3000}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3000}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?set in Coolify}
      SCENARIO_ENCRYPTION_KEY: ${SCENARIO_ENCRYPTION_KEY:?set in Coolify}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-disabled}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-disabled}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-placeholder}
      GROQ_KEY: ${GROQ_KEY:-placeholder}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-placeholder}
      OPENROUTER_BASE_URL: ${OPENROUTER_BASE_URL:-}
      GEMINI_BASE_URL: ${GEMINI_BASE_URL:-}
      S3_BUCKET: ${S3_BUCKET:-autonoma}
      S3_REGION: ${S3_REGION:-us-east-1}
      S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:-unused}
      S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:-unused}
    entrypoint: ["/bin/sh", "-lc"]
    command:
      # api dev uses tsx --env-file=../../.env; Node exits 9 if the file is
      # missing (env comes from the container), so create an empty one.
      - >
        cd packages/db && pnpm exec prisma migrate deploy && cd /app && touch .env && pnpm api
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  ui:
    build:
      context: .
      dockerfile: Dockerfile.coolify
    working_dir: /app
    environment:
      # Domain routing comes from Coolify's docker_compose_domains (set via API):
      # it generates the traefik router; traefik auto-picks ui's single exposed
      # port (3000). traefik already shares this app's network via coolify-proxy.
      API_PORT: "4000"
      API_PROXY_TARGET: http://api:4000
      ALLOWED_HOSTS: "*"
      VITE_API_URL: ""
      VITE_INTERNAL_DOMAIN: ${INTERNAL_DOMAIN:-autonoma.app}
    entrypoint: ["/bin/sh", "-lc"]
    command: ["pnpm ui -- --host 0.0.0.0"]
    depends_on:
      api:
        condition: service_started
    expose:
      - "3000"
    # Coolify's proxy is caddy-docker-proxy (not traefik). Its auto-domain emits
    # `{{upstreams}}` with NO port -> caddy dials the wrong port -> 502. Provide
    # explicit caddy labels with port 3000. No caddy_ingress_network: caddy
    # resolves ui via the single app network it shares (works for any app uuid).
    # Do NOT set docker_compose_domains, or Coolify adds a conflicting portless
    # reverse_proxy label.
    labels:
      caddy_0: https://autonoma.hostbun.cc
      caddy_0.encode: zstd gzip
      caddy_0.reverse_proxy: "{{upstreams 3000}}"
      # caddy resolves ui on this network (the app network coolify-proxy shares).
      caddy_ingress_network: l36uekt9kbxkpib4ojajw7ty

  worker-general:
    build:
      context: .
      dockerfile: apps/workers/general/Dockerfile
    environment:
      <<: [*worker-env, *github-env]
      SCENARIO_ENCRYPTION_KEY: ${SCENARIO_ENCRYPTION_KEY:?set in Coolify}
    restart: unless-stopped
    depends_on:
      - temporal

  worker-diffs:
    build:
      context: .
      dockerfile: apps/workers/diffs/Dockerfile
    environment:
      <<: [*worker-env, *github-env]
    restart: unless-stopped
    depends_on:
      - temporal

  worker-web:
    build:
      context: .
      dockerfile: apps/workers/web/Dockerfile
    environment:
      <<: *worker-env
      # Launch the image's own Chromium headless (no REMOTE_BROWSER_URL).
      HEADLESS: "true"
    # Run-once job pod: exits after one activity / 120s idle -> Coolify respawns.
    restart: always
    depends_on:
      - temporal

volumes:
  pgdata18:
  redisdata:
