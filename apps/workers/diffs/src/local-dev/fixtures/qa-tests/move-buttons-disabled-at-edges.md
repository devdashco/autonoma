---
name: Move Buttons Disabled At Board Edges
---

Navigate to the kanban board at http://localhost:3000. Locate the card titled "Research competitors" in the "To Do" column and assert that its "Move Left" button is disabled (the leftmost column has no column to its left). Then locate the card titled "User interviews" in the "Done" column and assert that its "Move Right" button is disabled (the rightmost column has no column to its right).
